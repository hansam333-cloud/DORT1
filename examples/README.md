# Examples

This folder contains sample inputs and outputs for each DORT agent.

The first example product should be:

```text
[피지컬핏] 쿨링 머슬 반팔티
```

Fitting image examples:

- `fitting-image-example.md`
- `fitting-image-product-replacement-example.md`
